<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Fan Animation</title>
  <style>
    body {
      background: #111;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .fan {
      width: 150px;
      height: 150px;
      border-radius: 50%;
      background: radial-gradient(circle at center, #555 30%, #222 70%);
      position: relative;
      animation: spin 1s linear infinite;
    }

    .blade {
      position: absolute;
      width: 10px;
      height: 70px;
      background: #0cf;
      top: 5px;
      left: 50%;
      transform-origin: bottom center;
    }

    .blade:nth-child(1) { transform: rotate(0deg)   translateY(-40px); }
    .blade:nth-child(2) { transform: rotate(120deg) translateY(-40px); }
    .blade:nth-child(3) { transform: rotate(240deg) translateY(-40px); }

    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
  </style>
</head>
<body>

  <div class="fan">
    <div class="blade"></div>
    <div class="blade"></div>
    <div class="blade"></div>
  </div>

</body>
</html
